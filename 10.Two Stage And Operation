#include <stdio.h>

int main() {
    // Operands
    int operand1 = 5;
    int operand2 = 7;

    // Perform AND operation
    int result = operand1 & operand2;

    // Display result
    printf("Result of AND operation: %d\n", result);

    return 0;
}
